{
  "name": "test-studio",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "bun server.ts",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "fast-xml-parser": "^5.7.3",
    "typescript": "^6.0.2",
    "yaml": "^2.8.3"
  },
  "devDependencies": {
    "bun-types": "^1.3.14"
  },
  "description": "A local test explorer and runner for Bun tests and Maestro flows.",
  "engines": {
    "bun": ">=1.3.0"
  }
}
